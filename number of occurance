class Solution:
    def countFreq(self, arr, target):
        count=0
        l=[]
        for i in range(len(arr)):
            if arr[i]==target:
                count+=i
                l.append(arr[i])
        return len(l)
