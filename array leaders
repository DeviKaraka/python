class Solution:
    def leaders(self, arr):
        l=[]
        max_right=-1
        for i in range(len(arr)-1,-1,-1):
            if arr[i]>=max_right:
                l.append(arr[i])
                max_right=arr[i]
        return l[::-1]
