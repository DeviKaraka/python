class Solution:
	def countOddEven(self, arr):
	
		even=[]
		odd=[]
		for i in range(len(arr)):
		    if arr[i]%2==0:
		        even.append(arr[i])
		    else:
		        odd.append(arr[i])
		return len(odd),len(even)
		    
