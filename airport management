# cook your dish here
t=int(input())
for i in range(t):
    n=int(input())
    arr=list(map(int,input().split()))
    d=list(map(int,input().split()))
    count=[0]*1440
    for j in range(n):
        count[arr[j]]+=1 
        count[d[j]]+=1  
    ans=max(count)
    print(ans)
